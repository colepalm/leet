/**
 * @param {number[]} nums
 * @return {number}
 */
var jump = function(nums) {
    const length = nums.length;
    const index = 0;
    while (length > index) {
        for (let i = 0; i < length; i++) {}
    }
};
